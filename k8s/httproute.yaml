apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: threema-web
spec:
  parentRefs:
    - name: kgw
      namespace: kgateway-system
  hostnames:
    - threema.nachtalb.ch
    - t.naa.gg
  rules:
    - backendRefs:
        - name: threema-web
          port: 80
